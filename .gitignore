# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
node_modules.nosync/
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next
/.next/
.next.nosync/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# Claude Code local config (skills/agents/launch.json are shared, rest is local)
.claude/*
!.claude/skills/
!.claude/agents/
!.claude/launch.json
memory/
next-48h.html

# Clickable mockup (Jun 2026) — local only, outdated vs live onboarding
public/demo.html

# Marketing/social media generated binaries — heavy, non-diffable, and
# regenerated on demand. Keep only the source .md (copy) and .svg (image
# source) in git; final renders (mp4/png/aiff/mp3/wav) live in Supabase
# Storage instead — see content/marketing/creatives/README.md.
content/marketing/creatives/**/*.mp4
content/marketing/creatives/**/*.mov
content/marketing/creatives/**/*.png
content/marketing/creatives/**/*.aiff
content/marketing/creatives/**/*.mp3
content/marketing/creatives/**/*.wav
immigrei-midias/**/*.mp4
immigrei-midias/**/*.mov
immigrei-midias/**/*.png
immigrei-midias/**/*.aiff
immigrei-midias/**/*.mp3
immigrei-midias/**/*.wav
!content/marketing/creatives/reel-template/reference-contact-sheet.png
